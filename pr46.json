{"body":"## Summary\n\n- Replaces the drop-newest queue (`queue.Queue`) with a drop-oldest `collections.deque`-based implementation\n- When the queue is full, the **oldest** message is evicted instead of dropping the **newest**, ensuring the most recent MQTT traffic always reaches the radio\n- Thread-safe via `threading.Lock` + `threading.Event` for efficient consumer wakeup\n\n## Problem\n\nOn high-volume regional MQTT brokers, inbound message rate can exceed the radio's drain rate. The old behavior dropped all new messages once the queue filled, causing complete loss of visibility into the mesh network until the queue drained.\n\n## Changes\n\n- `handlers/queue.py` — Rewrote `MessageQueue` with deque + lock + event, drop-oldest eviction, `qsize()` and `drain_all()` methods\n- `tests/test_queue_limit.py` — New tests for eviction behavior and logging\n- `tests/test_queue.py` — Updated to use new public API\n\n## Test plan\n\n- [x] All 10 queue tests pass (`pytest tests/test_queue.py tests/test_queue_limit.py`)\n- [x] Docker image builds successfully\n- [x] Deployed and monitored on a live instance: messages processed without errors or evictions, queue depth remained well below configured maximum","comments":[{"id":"IC_kwDOQs9MSs72lM_S","author":{"login":"NearlCrews"},"authorAssociation":"CONTRIBUTOR","body":"### Changes in a654371\n\nAddressed all three review comments and fixed a threading bug found during review:\n\n1. **Restored emoji in all log messages** — no reason to remove them; reverted to match the existing codebase style and minimize unrelated diff noise.\n\n2. **Added running eviction counter** — `self._eviction_count` increments on each eviction and is included in the warning log, e.g. `\"Queue full (5000/5000), evicted 12 total.\"` This makes it easy to spot a busy MQTT server in the logs without grepping timestamps.\n\n3. **Restored `(potentially unsafe)` warning and thread-safety comment** in `_send_to_radio` — the `_sendToRadioImpl` fallback lacks locking guarantees, so the original comment and warning text were important for maintainers.\n\n4. **Fixed event race condition in `_process_loop`** — the previous ordering (`_event.wait()` then `_event.clear()`) could lose a wakeup signal from `put()` if a message arrived between those two calls, adding up to 1s latency before the message was processed. Moved `_event.clear()` before `_get()` to follow the standard pattern for `threading.Event`-based signaling.\n\n5. **Restored `test_queue_warning_near_full` test** — the 80%-capacity warning path lost its test coverage when the old test was removed. Added it back along with a counter assertion in `test_queue_full_logs_eviction`.\n\n6. **Sanitized PR body** — replaced real deployment metrics with generic placeholders.\n\nAll 10 tests pass.\n\n🤖 Generated with [Claude Code](https://claude.ai/code)","createdAt":"2026-03-26T17:46:36Z","includesCreatedEdit":false,"isMinimized":false,"minimizedReason":"","reactionGroups":[],"url":"https://github.com/LN4CY/mqtt-proxy/pull/46#issuecomment-4136947666","viewerDidAuthor":false}],"reviews":[{"id":"PRR_kwDOQs9MSs7uoyBH","author":{"login":"LN4CY"},"authorAssociation":"OWNER","body":"","submittedAt":"2026-03-25T03:48:12Z","includesCreatedEdit":false,"reactionGroups":[],"state":"CHANGES_REQUESTED","commit":{"oid":"9d341683f7559eb6afd515f161b3dbeaacb1f47f"}}],"state":"OPEN","title":"feat: drop-oldest queue eviction to prevent message loss"}
